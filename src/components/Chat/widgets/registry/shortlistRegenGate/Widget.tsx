"use client";

// REPLAY-ONLY stub: nothing emits shortlist_regen_gate anymore — the shortlist
// board replaced the gate (an open negotiation just re-shows; a `direction`
// re-seeds). A pre-board session whose LAST message was a live gate mounts
// this and gets nothing; re-entering the company puts the board up instead.
export function ShortlistRegenGateWidget() {
  return null;
}
