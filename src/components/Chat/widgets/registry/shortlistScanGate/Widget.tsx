"use client";

// REPLAY-ONLY stub: nothing emits shortlist_scan_gate anymore — the shortlist
// board replaced the gate (un-read roles just show as a "not read yet" tier).
// A pre-board session whose LAST message was a live gate mounts this and gets
// nothing; re-entering the company puts the board up instead.
export function ShortlistScanGateWidget() {
  return null;
}
